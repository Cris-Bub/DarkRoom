# Agent Entry Point

Read `Agent.md` before changing this repository. This compatibility file exists because some coding agents look for `AGENTS.md` by convention, while the canonical project guidance lives in `Agent.md`.
